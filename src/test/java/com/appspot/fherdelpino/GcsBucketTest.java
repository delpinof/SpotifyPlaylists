package com.appspot.fherdelpino;

import java.io.IOException;

import org.junit.Test;

public class GcsBucketTest {
	
	@Test
	public void getClientId() throws IOException {
//		String jsonPath = "/Users/fherdelpino/workspaces/GitHub/google/service-account-key/GCP_service-account-key_fherdelpino-owner.json";
//		GoogleCredentials credentials = GoogleCredentials.fromStream(new FileInputStream(jsonPath))
//		          .createScoped(Lists.newArrayList("https://www.googleapis.com/auth/cloud-platform"));
//		    Storage storage = StorageOptions.newBuilder().setCredentials(credentials).build().getService();
//
//		    System.out.println("Buckets:");
//		    Page<Bucket> buckets = storage.list();
//		    for (Bucket bucket : buckets.iterateAll()) {
//		      System.out.println(bucket.toString());
//		    }
		    
		    
//		GcsService gcsService = GcsServiceFactory.createGcsService();
//		GcsFilename filename = new GcsFilename("fherdelpino-template", "counts-00000-of-00003");
//		GcsInputChannel readChannel = null;
//	    BufferedReader reader = null;
//	    String line;
//	    try {
//	      readChannel = gcsService.openReadChannel(filename, 0);
//	      reader = new BufferedReader(Channels.newReader(readChannel, "UTF8"));
//	      
//	      while ((line = reader.readLine()) != null) {
//	    	  System.out.println(line);
//	      }
//	    } finally {
//	      if (reader != null) { reader.close(); }
//	    }
//	    assertNotNull(line);
	}

}
